#ifndef STACK_TRIE_HEADER_H
#define STACK_TRIE_HEADER_H 1

#include <fasttrack.h>
//
//template <class LockT>
//class FasttrackTest : public Fasttrack<LockT> {
// public:
//  // helper funtion for a unit_test
//  void clearVarState(std::size_t addr) {
//#if PROF_INFO
//    PROF_FUNCTION();
//#endif
//    auto it = vars.find((size_t)(addr));
//    if (it != vars.end()) {
//      vars.erase(it);
//    }
//  }
//};

#endif
